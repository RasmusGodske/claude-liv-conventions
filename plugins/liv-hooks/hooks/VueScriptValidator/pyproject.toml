[project]
name = "vue-script-validator"
version = "1.0.0"
description = "Pre-tool hook to validate Vue files use <script setup lang='ts'>"
requires-python = ">=3.10"
dependencies = [
    "claude-hook-utils>=0.3.0",
]
